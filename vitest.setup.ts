import { vi } from 'vitest'

// ==================== Глобальные моки браузера ====================

// Минимально необходимые глобальные объекты
global.GM_getValue = vi.fn()
global.GM_setValue = vi.fn()
global.GM_addStyle = vi.fn()

// ==================== Моки модулей ====================
// Используем абсолютные пути (от корня проекта), чтобы моки не ломались при перемещении файлов

vi.mock('#src/classes/Logger', () => ({
  Logger: {
    log: vi.fn(),
    info: vi.fn(),
    warn: vi.fn(),
    error: vi.fn(),
  },
}))

// switchKeyboardLayout НЕ мокаем — тестируем реальную реализацию,
// так как мок полностью дублировал исходную функцию и не имел смысла
